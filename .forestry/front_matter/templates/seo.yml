---
label: SEO
hide_body: true
fields:
- name: seo
  type: field_group
  config: {}
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
  - name: description
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: Description
  - name: open_graph_image
    type: file
    config:
      maxSize: 64
    label: Open Graph Image
    description: 'This is the image that will appear when this page is shared to a
      social media or similar platform. Image should be a minimum 1200w x 630h in
      pixels. '
  label: SEO
