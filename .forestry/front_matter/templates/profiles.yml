---
label: Profiles
hide_body: true
fields:
- name: name
  type: text
  config:
    required: true
  label: Name
- name: bio
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html-blocks
  label: Bio
- name: photo
  type: file
  config:
    maxSize: 64
  label: Photo
