export interface Content {
  content: Data;
}

export interface Data {
  heading: string;
  body: string;
  button_text: string;
  button_url: string;
  image: string;
  image_alt_text: string;
  image_background_color: string;
}
